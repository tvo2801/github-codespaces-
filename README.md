# github-codespaces-
This is for a demo

'which virtualenv
virtualenv --help

virtualenv ~/.venv
source ~/.venv/bin/activate
which python
'

'vim ~./bashrc
insert: Source venv
source ~/.venv/bin/activate'

'source venv'

